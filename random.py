import random
a=random.randint(0,100)
print("random number between 100 annd 0",a)

#  random.random() used to create random number inbtw floating points.
#  random.choice(sequence) used to create random numbers in list, tuple,string
#  random.shuffle(sequence/name of list etc) gives shuffled list
#  random.seed( number) gives a random number and repeats the same at the specified repeated loop. 
#  random.randrange(low,uprange) without including the range given......... to give skip of 2 then (1,10,2) odd/even
# 
#
#
#